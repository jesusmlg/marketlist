<%= form_for @product do |f| %>
	<%= f.text_field :name ,id:'producto_to_add' %> <%= f.submit 'Agregar', class: 'btn btn-primary' %>
<% end %>

	<!-- <%= submit_tag 'Comprado', class: 'btn btn-danger', id:'buy' %> -->
	